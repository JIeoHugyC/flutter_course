import 'package:flutter/material.dart';

class MyWidget extends StatelessWidget {
  const MyWidget({
    super.key,
  });

  static const double sideLength = 200;

  @override
  Widget build(BuildContext context) {
    return Text('Replace this widget');
  }
}
