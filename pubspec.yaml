name: flutter_course
description: A project for the Flutter course
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.19.1 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  googleapis: ^5.0.0
  faker_dart: ^0.1.11
  http: ^0.13.5
  geolocator: ^9.0.2
  geocoding: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  golden_toolkit:
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

